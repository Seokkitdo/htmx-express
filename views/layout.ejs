<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title></title>
    <link href="/css/style.css" rel="stylesheet" />

    <script
      src="https://unpkg.com/htmx.org@1.9.9"
      integrity="sha384-QFjmbokDn2DjBjq+fM+8LUIVrAgqcNW2s0PjAxHETgRn9l4fvX31ZxDxvwQnyMOX"
      crossorigin="anonymous"
    ></script>
    <!-- 여기에 Tailwind CSS 또는 다른 스타일시트 연결 -->
  </head>

  <body class="flex h-screen bg-gray-100">
    <div
      hx-get="/dashboard/user"
      hx-trigger="load"
      hx-target="main"
      hx-swap="innerHTML"
    ></div>
    <div class="flex-1 flex flex-col">
      <header class="p-4 bg-red-600">
        <!-- 헤더 내용 -->
        헤더
      </header>

      <div class="flex flex-1">
        <aside class="w-1/6 p-4 bg-red-500">
          <!-- 사이드바 내용 -->
          <ul class="flex flex-col gap-4">
            <li class="">
              <button
                hx-get="/dashboard/user"
                hx-trigger="click"
                hx-target="main"
                hx-swap="innerHTML"
              >
                사용자
              </button>
            </li>
            <li class="">
              <button
                hx-get="/dashboard/notification"
                hx-trigger="click"
                hx-target="main"
                hx-swap="innerHTML"
              >
                알림
              </button>
            </li>
            <li class="">
              <button
                hx-get="/dashboard/device"
                hx-trigger="click"
                hx-target="main"
                hx-swap="innerHTML"
              >
                기기
              </button>
            </li>
          </ul>
        </aside>
        <div class="flex flex-col w-full">
          <main id="main" class="flex-1 overflow-auto">
            <!-- 페이지별 내용이 들어갈 부분 -->
          </main>
          <footer class="p-3 bg-red-600 text-2xs">
            <!-- 푸터 내용 -->
            푸터
          </footer>
        </div>
      </div>
    </div>
  </body>
</html>
